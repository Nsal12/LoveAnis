name: vps

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run VPS instance 1
        run: curl -sSf https://sshx.io/get | sh -s run

      - name: Run VPS instance 2
        run: curl -sSf https://sshx.io/get | sh -s run

      - name: Run VPS instance 3
        run: curl -sSf https://sshx.io/get | sh -s run
